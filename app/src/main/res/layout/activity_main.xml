<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/btn_explicit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="1. Explicit Intent (To Second Activity)"
        android:onClick="startSecondActivity"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="32dp"
        android:layout_marginStart="32dp" />

    <Button
        android:id="@+id/btn_implicit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="2. Implicit Intent (Open Web Browser)"
        android:onClick="openWebPage"
        app:layout_constraintStart_toStartOf="@id/btn_explicit"
        app:layout_constraintTop_toBottomOf="@id/btn_explicit"
        android:layout_marginTop="16dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Kiểm tra Vòng đời Activity trong Logcat"
        android:textSize="14sp"
        app:layout_constraintStart_toStartOf="@id/btn_explicit"
        app:layout_constraintTop_toBottomOf="@id/btn_implicit" android:layout_marginTop="32dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>